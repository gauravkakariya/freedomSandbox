public with sharing class CreateAccountThroughSqlHandler 
{
    public static void createAccount(List<Account> lstCreateAccout)
    {
    	system.debug('TRigger Fired');
    	system.debug('***************************************'+lstCreateAccout);
       //  Contact con = new Contact();
    	//for(Account objac : lstCreateAccout)
    	//{
    	//	if(objac.BillingStreet != NULL)
    		{
    		//  con.Name = objac.LastName;
    		}
    	//	lstAccount.add(objac);
    //	}
    //	 update con;
    	
    }
}